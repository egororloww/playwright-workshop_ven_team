export type CarType = {
  id: number;
  make: string;
  model: string;
  price: string;
  normalImageUrl: string;
  normalImageKey: string;
  hoverImageUrl: string;
  hoverImageKey: string;
  isAvailable?: boolean;
};
